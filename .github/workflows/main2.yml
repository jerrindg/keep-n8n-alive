name: Keep n8n Alive - 2

on:
  schedule:
    - cron: '*/4 * * * *'  # Runs every 4 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep n8n awake
        run: curl -I https://n8n-tqqo.onrender.com || true
