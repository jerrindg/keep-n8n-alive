name: Keep n8n Alive - 2 Min

on:
  schedule:
    - cron: '*/2 * * * *'  # Runs every 2 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep n8n awake
        run: curl -I https://n8n-tqqo.onrender.com || true
